const calculateTime = (dayOfWeek, startTimeInMinutesAfterMidnight, endTimeInMinutesAfterMidnight, standardRate, overtimeMultiplier, doubleTimeMultiplier) => {
  // 1. Calculate total time worked
  const totalMinutesWorked =  endTimeInMinutesAfterMidnight - startTimeInMinutesAfterMidnight

  // 2.

  // 3.

  // 4.

  const timesAndPayObject = {
    standardMinutes: null,
    overtimeMinutes: null,
    doubleTim1eMinutes: null,
    totalPay: null,
  } 
  return timesAndPayObject
}

export { calculateTime }